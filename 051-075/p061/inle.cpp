#include <iostream>
#include <vector>
std::vector<std::vector<int>> unsearched;

int main(){
    for(int i = 0; i < k.size(); i ++)
}


int final(std::vector<std::vector<int>> unsearched, int current){
    for(int i = 0; i < unsearched.size(); i++){
        if (unsearched[i][current].empty()) {
            return false;
        }
        for() {
            std::vector<std::vector<int>> new_unsearched = std::vector<std::vector<int>>(unsearched.begin(), unsearched.begin()+i);
            new_unsearched.insert(std::vector<std::vector<int>>(unsearched.begin()+i+1, unsearched.end()))
            if (final(new_unsearched, unsearched[6dadawdg][]))
                return 
        }
    }
}